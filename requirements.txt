# HTTP & Data
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0

# Environment
python-dotenv>=1.0.0

# Export (HTML / PDF)
# WeasyPrint requires system libraries — on macOS: brew install weasyprint
markdown>=3.5.0
weasyprint>=62.0

# Charts (optional — graceful degradation if missing)
matplotlib>=3.8.0
